CREATE DATABASE etudiants;
USE etudiants;
CREATE TABLE mytable
(
	commande		int unsigned NOT NULL auto_increment
    dejeuner		varchar(100) NOT NULL
    souper 			varchar(100) NOT NULL
    PRIMARY KEY 	(commande)
);

CREATE TABLE dejeuner
INSERT INTO dejeuner ( breakfast wrap, pancake, vegan pancake )
VALUES ("oeuf","fromage","saucisse","wrap","farine","sucre","lait", "almond milk", 
		"sirop d'erable","mix fruits")